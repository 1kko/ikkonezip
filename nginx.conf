server {
    listen 3000;
    listen [::]:3000;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    server_tokens off;

    # SPA fallback: any unmatched route serves index.html so the React
    # router can take over.
    location / {
        include /etc/nginx/snippets/security_headers.conf;
        try_files $uri $uri/ /index.html;
    }

    # Vite emits content-hashed filenames in /assets — safe to cache forever.
    location /assets/ {
        include /etc/nginx/snippets/security_headers.conf;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }

    # Source maps are emitted for local debugging but must never be served
    # publicly.
    location ~ \.map$ {
        return 404;
    }

    # The entry point and service worker must always be revalidated so users
    # pick up new builds immediately.
    location = /index.html {
        include /etc/nginx/snippets/security_headers.conf;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    location = /sw.js {
        include /etc/nginx/snippets/security_headers.conf;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    location = /registerSW.js {
        include /etc/nginx/snippets/security_headers.conf;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    location = /manifest.webmanifest {
        include /etc/nginx/snippets/security_headers.conf;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        types { } default_type "application/manifest+json";
    }

    gzip on;
    gzip_vary on;
    gzip_min_length 256;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/manifest+json
        image/svg+xml;

    # Don't log the healthcheck — keeps access logs readable.
    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }
}
