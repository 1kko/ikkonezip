## Summary

<!-- 1-3 bullets: what changed and why -->

## Test plan

<!-- How did you verify this? Check what applies and add notes. -->

- [ ] `npm run lint` passes
- [ ] `npm run test` passes
- [ ] `npm run build` succeeds
- [ ] Manually verified in browser (if UI change)
- [ ] Korean filename round-trip still works (if touching normalize/zip path)

## Notes for reviewer

<!-- Anything non-obvious: trade-offs taken, alternatives considered, follow-ups deferred. -->
